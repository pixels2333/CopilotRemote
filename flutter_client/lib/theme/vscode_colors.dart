import 'package:flutter/material.dart';

class VSCodeColors {
  // Brand Colors (Copilot Blue)
  static const Color accentCode = Color(0xFF007ACC);
  static const Color accentCopilot = Color(0xFF0632E5);
  static const List<Color> copilotGradient = [
    Color(0xFF0632E5),
    Color(0xFF4285F4),
  ];

  // Glassmorphism Params
  static const double glassBlur = 12.0;
  static const double glassOpacity = 0.65;
}
